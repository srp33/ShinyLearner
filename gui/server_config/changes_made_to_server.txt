### Changes to have the server run automatically
# from https://github.com/rstudio/shiny-server/wiki/Building-Shiny-Server-from-Source
sudo apt-get -y upstart
sudo wget\
  https://raw.github.com/rstudio/shiny-server/master/config/upstart/shiny-server.conf\
    -O /etc/init/shiny-server.conf

### These steps were run, but they are not necessary
sudo apt-get -y install libcurl4-openssl-dev #unncessesary for now
tried and failed to install readr (and Rcpp) in R (not using readr for now)
sudo chmod 755 /etc/init.d/shiny-server
update-rc.d shiny-server defaults
sudo wget https://raw.githubusercontent.com/rstudio/shiny-server/master/config/init.d/debian/shiny-server -O /etc/init.d/shiny-server


